# 前端环境变量示例（Vercel 部署时在平台后台填入真实值）
# 本文件可安全提交到 Git；真实 .env 文件已被 .gitignore 忽略
# 注意：只有 VITE_ 前缀的变量才会暴露给前端代码

# ─────────────────────────────────────────────
# VITE_API_BASE：线上后端的真实 API 基准地址
# ─────────────────────────────────────────────
# 本地开发：不设时默认 "/api/v1"，由 Vite 代理转发到 localhost:3001
# 线上部署：填 Railway 后端的真实地址（带 /api/v1 前缀）
#           Vercel 是纯静态托管，没有开发代理，必须直连这个云端地址
# VITE_API_BASE=https://mini-hot-hub.up.railway.app/api/v1
